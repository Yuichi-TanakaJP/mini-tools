# 優待の共通データ層（切替工程3）

対象: 優待メモ、カレンダー/候補、ダッシュボード、期限・残高と依存通知。
2026-09-10時点で共通処理はPR593で完了。カレンダー・メモ基本編集・タグ・履歴・残高/期限の基本操作・ダッシュボードを隔離検証用に接続。
本番の保存先は変わらない。

## 全体の位置づけ

利用開始までの6工程は固定。工程数は作業量が均等ではないので割合へ換算しない。

| 工程 | 状態 |
|---|---|
| 1 全件読み取りAPI | stock-notes PR188でDB適用・検証済み |
| 2 日常更新24操作 | stock-notes PR190/191でDB適用・検証済み |
| 3 共通取得・保存・キャッシュ | PR593で完了。コード・契約テスト・認証連携テスト |
| 4 既存画面接続 | 進行中。カレンダー・メモ基本編集/月別数値/タグ/履歴・残高/期限の基本操作・期限帳の期間/並べ替え/カード・表表示/現残高円換算・ダッシュボード・DB全件出力/照合/確認付き復元を隔離検証接続。残件は下記4-A〜4-Fの6グループ |
| 5 スマホ/PC/ChatGPT UAT・export/復旧 | 未着手 |
| 6 更新停止中の最新差分照合・read/write同時切替・旧同期停止 | 未着手 |

工程3の完了だけを「日常利用の切替完了」と呼ばない。高度なRealtimeや大規模UI再編を
この6工程の完了条件に追加しない。
[DB側の正本計画](https://github.com/Yuichi-TanakaJP/stock-notes/blob/main/docs/yutai-minitools-cutover.md)も参照。

## 工程4の終了範囲

### 工程4の残件台帳（2026-09-10、PR606後）

これまでの「従来UI互換等」は作業範囲・終了点が不明瞭だった。以下は既存残件の整理であり、
新機能追加や工程5/6の前倒し・完成条件の縮小ではない。残件は以下の **6グループ** で管理する。
6グループは6PR・6ターン・同じ作業量を意味しない。4-E/Fは接続・合成データでの自動検証が完了し、残りは4-A/B/C/Dの4グループ。
追加発見は該当グループの不具合/不足として根拠と影響を記録し、新機能を切替条件に混ぜない。

| ID | 残件と対象 | このグループの終了条件 |
|---|---|---|
| 4-A | 期限帳の累計受取・利用・失効集計 | 現履歴、移行由来履歴、単位変更前履歴を区別して集計できる。初期値や単位変更を二重付与にせず、補充・取消・訂正・アーカイブ・NULL額面のテストと表示が通る |
| 4-B | メモの複数選択操作・月替わりアーカイブ | 選択対象と権利年月を確認して保存できる。事前仕込みは誤って終了しない。部分成功/競合/応答不明でも成功分を再実行しない |
| 4-C | 旧形式の入出力導線 | 旧メモ/期限帳入力の対応形式を列挙し、全件差分確認・保全・再実行/競合防止を経てDBへ保存できる。旧LocalStorageへの迂回更新を残さない。DB全件出力/復元そのものはPR602/604で接続済み |
| 4-D | 期限帳の画像スキャン結果の保存 | 既存premium境界を保持し、読取結果の修正・追加/統合確認からDBへ保存する。取消・再試行で重複や旧保存先への書込を起こさない |
| 4-E | **接続完了** メモの既存表示接続 | 権利月/仕込み月・タグ絞込・並べ替え・本人の銘柄に対応する信用売り残高の表示/取得導線をDBメモに接続。NULL/0・年またぎ・0/101銘柄・取得拒否・旧保存先不変を合成データで検証。実機/本番切替は工程5/6 |
| 4-F | **接続完了** 優待に依存する既存通知 | ホーム側の優待期限を同じDB取得結果へ接続し、ログアウト/取得失敗で古い本人情報や誤った0件を表示しない。既存実装に仕込み通知はなく、台帳の記述を訂正（ホーム通知decision-log参照）。高度な新規通知は対象外 |

現在の残高換算はPR606で完了（期限内・今月期限・期限切れ）。4-Aの生涯累計とは別。
工程4の終了判定は上の6行の受入確認と既接続画面の回帰確認。テスト・修正を新たなグループとして水増ししない。
次は工程5のスマホ/PC/ChatGPT往復・復旧検証、最後が工程6の差分照合と本番同時切替。
工程5/6を省略して公開利用可とはしない。復元APIの本番適用も、その検証/切替の前提として追跡する。

### 4-A 調査済みの取得不足と実装順

2026-09-11: 4-A〜Dをまとめて実装。stock-notes PR198をマージし、確認可能な受取/利用の本人限定APIとPR195の復元APIを本番DBへ設置。既存11テーブル（旧tool_dataを含む）の件数・全件fingerprint不変を確認。復元先設定は空で、実際の復元と画面の本番切替は未実施。
単位変更の初期値を再計上せず、旧adjusted・由来不明初期値・額面変更/未設定は不明とする。
累計失効は原データにイベントがないため未確定と表示する。完全累計の完成条件を黙って縮小しない。
4-Bは権利年月明示の複数選択、4-Dはpremiumを維持するスキャン確認保存を接続。
月替わりの過去仕込みは既存年月別cycleをそのまま表示し、移動・削除・状態書換えはしない。
4-Cは旧端末バックアップの既知優待キー/期限帳v2配列から追加案を作り、復元の保全・照合を利用する。
未対応の追加情報・既存データとの差分・権利年不明は保存前に停止する。自動上書きではない。
この追加だけで本番移行の完了とはしない。最新スマホ原本の照合と実機往復は別途必須。

画面側だけの合算では生涯累計を保証できないため、表示のみの仮集計を完了扱いにしない。

1. `change_reward_mode` は変更前のrewardと利用履歴を `stock_notes_yutai_events.payload` に保存し、
   現在の利用履歴を取り除いて初期値/残高を新単位で設定する。現workspace APIはこの監査を返さない。
2. 初回移行の `yutai_migration_reward_plan.py` は旧履歴を `event_type=adjusted` として保存する。
   `consumed/restocked` だけの合算では旧利用分が欠落し、全adjustedを利用/付与と決めつけると訂正を誤計上する。
3. 次の実装はDB側の本人限定の集計用読み取り契約から行う。監査の全payloadや他ツールのデータを
   ブラウザへ公開せず、必要な履歴区分・金額・集計不能の理由だけを投影する設計とする。
4. 旧履歴の由来、単位変更、取消、削除/復元の扱いを保全済みデータと照合する。
   原文から確定できない分類は推測せず不明として扱い、完全な累計と誤認させない。
5. 正常/異常/境界のDB検証後にMiniToolsの受信契約・集計表示・UATを接続する。
   本番DBへの適用は別途変更内容と保全対象を示して実施する。今回は適用していない。

コード根拠（stock-notes main）:
[単位変更](https://github.com/Yuichi-TanakaJP/stock-notes/blob/main/supabase/migrations/20260909040625_stock_notes_yutai_reward_mode_change.sql)、
[移行履歴](https://github.com/Yuichi-TanakaJP/stock-notes/blob/main/app/yutai_migration_reward_plan.py)、
[現読み取り](https://github.com/Yuichi-TanakaJP/stock-notes/blob/main/supabase/migrations/20260909004349_stock_notes_yutai_workspace_read.sql)。

## 契約と入口（実装済み共通層）

- `lib/yutai/contracts.ts`: workspace、24種類のcommand、返却値の型と受信検証。
- `lib/yutai/repository.ts`: テスト可能な共通キャッシュ・保存状態管理。
- `lib/yutai/runtime.ts`: 既存Supabase Auth sessionとRPC、ブラウザ復帰イベントの接続。
- `lib/yutai/browser.ts`: 遅延生成のブラウザ共有インスタンスと`useYutaiWorkspace(month)`。
  カレンダー検証モードから利用する。無効時は通信しない。SSRでは本人データを作らない。

読み取りは `stock_notes_get_yutai_workspace({ p_month: 1..12 })`。
保存は `stock_notes_record_yutai_command({ p_input: command })`。
旧context、初回importer、table直接書き込み、旧tool_data同期は使わない。
DBルールの重複実装を避け、入力の業務制約・権限・revision・transactionの正本はDB側。
受信時はschema、月、全配列、件数、行fieldの型、enum、revisionを検証し、欠落を空配列へ変換しない。
未設定方針、NULLと0、株数・優待価値、全年度cycle、archive、全履歴を保持する。
月別selectionは毎年共通の月、cycleは権利年月。両者を混同しない。

## 本番切替の保護

`NEXT_PUBLIC_YUTAI_DB_CANONICAL=true` をビルド時に設定し、6つの画面フラグを一緒に有効化する。
同じフラグで旧優待7キーの同期送信/受信と端末取込による上書き/削除を止める。
旧版クライアントからの `/api/sync` 書込は409で全要求を拒否し、再読み込みを案内する。
他ツールの同期は維持する。旧LocalStorageとtool_dataは保管し、端末exportは旧保管用と表示する。
フラグ未設定では従来動作を維持する。本番正本モードでは共通ステータスに「Supabase保存」と表示し、検証環境だけに未切替の注意を表示する。
2026-09-11、ユーザー承認後にProductionで有効化し、再デプロイ済み。
実ログイン状態のメモ画面で97銘柄を読み取り、タグ名の保存・再読み込み・元の名前への復元を確認した。
全体復元の本番試験やスマホ実機確認まで完了したことを意味しない。
2026-09-11、ユーザーは9月1日以降未使用と確認し、同ファイルを最新原本として指定。
原本401,120bytesのSHA-256がDBの保全原本と一致し、再インポートは不要。

## キャッシュと認証

保存案内は画面を占有しない小さな状態行とし、保存先・旧データの説明・取得日時は「保存について」にまとめる。
通常時の技術的説明を減らしたいという利用者の要望による。エラー・競合・古い表示・復元停止は折り畳まず、確認が必要な状態を隠さない。

- Supabaseのみ正本。キャッシュはメモリ内のみで、再読み込みやアプリ終了で消える。
  LocalStorage / IndexedDBにコピーも未送信操作も保存しない。
- ログイン本人と選択月ごとに分離。別ユーザーへの変更・logoutで全破棄。
  一度logoutして同じ本人に戻った場合も古い通信・保存要求を無効化。
- Auth callbackでは同期的な状態変更のみ。RPCはcallbackを抜けてから実行する。
- 各通信直前にsession ownerを確認し、確認したaccess tokenをその要求に固定する。
  途中でアカウントが変わっても別の本人として送らない。古い応答を新しい画面へ返さない。
- 同じ月への並行取得をまとめる。通常の画面遷移は30秒以内の新鮮なcacheを再利用する。
  30秒は初期の調整可能値で、リアルタイム保証ではない。
- focus、表示復帰、online復帰、明示`load(month, true)`で表示中の月を再取得。
  保存後は全月cacheを古い状態とし、表示中の月と保存元の月を再取得する。
- foregroundに居続ける別端末の更新をpushでは受けない。必要時は手動更新。
- RPC通信は15秒で中断。タイムアウトした保存は未保存と断定しない。
- オフラインは取得済みメモリcacheの閲覧のみ。再起動後のoffline閲覧は保証しない。
  `fetchedAt`と`stale`をUIに表示し、空データと認証エラーを区別する。

## 保存の組み込み方（工程4で必須）

1. 利用者が確認した編集内容と、その編集開始時の`expected_revision`から`prepare(draft)`。
2. 返った`PreparedCommand`をフォーム側で保持して`save(prepared, selectedMonth)`。
3. 同じ操作の二重クリック・不明結果の再確認では同じオブジェクトを使う。
   毎回`prepare`を呼び直して新しいrequest IDを作ってはいけない。
4. 全ての保存結果を次の表で分岐する。古い画面を成功表示で閉じない。

| 結果 | 意味・UIの扱い |
|---|---|
| saved | DB成功＋最新データ再取得完了。フォームを閉じられる |
| saved_refresh_failed | 保存済み、表示更新だけ失敗。「保存済み・再取得失敗」と表示し読み取りを再試行 |
| not_saved | offline、session事前不一致、DBの競合/入力拒否。理由を表示 |
| uncertain | 通信応答不明・不正応答・送信中の本人切替。未保存とも成功とも断定しない |

`uncertain`は同じprepared要求を再確認できる。DBの同一request再送機構により二重加算しない。
ページ終了やlogoutでprepared要求も使用できなくなるため、不明結果があるまま閉じない導線を工程4で用意する。
閉じた場合は最新データ/監査で確定結果を確認してから新規入力する。自動再送・offline queueはない。
競合時は最新データを取得するが、revisionを勝手に差し替えて保存し直さない。
成功済みrequestの再送応答は過去の結果なので、その応答だけで画面を更新せず必ず再取得する。
削除結果はafterとrevisionがnull。通常更新のrevisionと混同しない。
SQLエラー本文は画面へ返さず、安全な分類メッセージだけを返す。

## 検証と復旧

[全件出力・照合・復元](../tools/data-transfer.md)は現在のworkspaceを保全する機能であり、削除済みデータ/操作監査/DBスキーマ等の完全backupではない。復元は独立フラグ下で専用RPCへ接続する。本番適用・実端末UATは未実施。

`npm test -- lib/yutai`で契約、並行取得、競合、二重送信、失敗、本人切替を検証する。
[UAT手順](../../uat/yutai-repository.md)と
[判断理由](../../decision-log/2026-09-09-yutai-authoritative-repository.md)を参照。
この追加はDB migrationや本番writeを含まない。カレンダーの検証モードだけ保存処理を呼び替える。
詳細は[カレンダー仕様](../tools/yutai-candidates.md)。全画面の接続完了まで工程4を完了にしない。
問題時はこの追加commitのrevertで戻せる。既存LocalStorage、旧tool_data、移行backupは残る。
画面接続後に新DBへ書いたデータの復旧は工程5/6で別途照合し、旧backupを無条件上書きしない。

公式確認: [RPC](https://supabase.com/docs/reference/javascript/rpc)、
[Authイベント](https://supabase.com/docs/reference/javascript/auth-onauthstatechange)。
